# Rust
src-tauri/target/
src-tauri/gen/

# Node
node_modules/
dist/
coverage/

# Generated bindings
src/lib/bindings.ts

# OS
.DS_Store

# Superpowers brainstorm scratch
.superpowers/

# Claude Code local config (per-user paths, allowlists, etc.)
# Default: ignore everything under .claude, then re-include the
# project-shared bits (settings.json + scripts/) so settings.local.json
# and other per-user files stay out.
.claude/*
!.claude/settings.json
!.claude/scripts/
