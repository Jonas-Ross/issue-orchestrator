## Screenshots

Drop PNGs here with these exact filenames so the top-level `README.md`
embeds resolve. Capture against a real session (running `claude` in a
worktree) so the status pills are showing real state.

| File | What it shows |
|---|---|
| `main-window.png` | Full window with sidebar expanded, two or three sessions in different statuses (running/needs-input/idle), one terminal active. The hero image — make this one count. |
| `issue-picker.png` | The issue picker modal open over the main window, with a repo's open issues listed. |
| `command-palette.png` | The command palette (⌘K) open showing fuzzy results. |
| `settings-panel.png` | The settings panel (⌘,) with repos configured. |
| `setup-panel.png` | The first-run setup panel showing the hook snippet. |

Capture at 2x retina (Cmd-Shift-4 on macOS) so the screenshots stay sharp
on a HiDPI README render. Trim any chrome you don't want shown
beforehand.
